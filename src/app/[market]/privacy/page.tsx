import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getMarket,isMarketKey,marketRoutes } from "@/data/markets";
import styles from "../../legal.module.css";
export async function generateMetadata({params}:{params:Promise<{market:string}>}):Promise<Metadata>{const{market}=await params;const cfg=getMarket(market);if(!cfg)return{};return{title:`Privacy Policy | ${cfg.brandShort} ${cfg.marketLabel}`,description:`Privacy policy for ${cfg.brandShort} and ElectronicRecycle.net.`,alternates:{canonical:`/${market}/privacy`}}}
export default async function Page({params}:{params:Promise<{market:string}>}){const{market}=await params;if(!isMarketKey(market))notFound();const cfg=getMarket(market)!;const r=marketRoutes(market);return <div className={styles.page}><section className={styles.hero}><div className="ever-shell"><h1>Privacy Policy</h1><p>How ElectronicRecycle.net and {cfg.brandShort} handle information submitted through this local market.</p><span className={styles.updated}>Last updated August 17, 2026</span></div></section><section className={styles.body}><div className={`ever-shell ${styles.content}`}><section><h2>Information you submit</h2><p>Forms may collect your name, company, email, phone number, ZIP code, item details, notes, photos, and inventory files when you choose to provide them.</p></section><section><h2>How it is used</h2><p>Information is used to answer questions, evaluate pickup requests, confirm recycling options, and operate the website. Uploaded files are used only to review the request they accompany unless a different use is explicitly agreed.</p></section><section><h2>Email delivery</h2><p>Website forms may use a transactional email provider such as Resend to deliver requests to the appropriate local market contact.</p></section><section><h2>Technical data</h2><p>Hosting providers may process standard logs such as IP address, browser information, and request timestamps for security and reliability. If analytics or advertising tools are added later, this policy should be updated before those tools are enabled.</p></section><section><h2>Contact</h2><p>Privacy questions can be submitted through the <Link href={r.contact}>contact page</Link>.</p></section></div></section></div>}
