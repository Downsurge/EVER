import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getMarket,isMarketKey,marketRoutes } from "@/data/markets";
import styles from "../../legal.module.css";
export async function generateMetadata({params}:{params:Promise<{market:string}>}):Promise<Metadata>{const{market}=await params;const cfg=getMarket(market);if(!cfg)return{};return{title:`Hard Drive & Data-Bearing Device Recycling in ${cfg.marketLabel} | ${cfg.brandShort}`,description:`Guidance for hard drives, SSDs, servers, computers, and other data-bearing devices being recycled with ${cfg.brandShort}.`,alternates:{canonical:`/${market}/data-security`}}}
export default async function Page({params}:{params:Promise<{market:string}>}){const{market}=await params;if(!isMarketKey(market))notFound();const cfg=getMarket(market)!;const r=marketRoutes(market);return <div className={styles.page}><section className={styles.hero}><div className="ever-shell"><p className="ever-eyebrow">DATA-BEARING DEVICES</p><h1>Know what happens before you hand over a drive.</h1><p>Hard drives, SSDs, computers, laptops, servers, phones, and other electronics can contain sensitive data. {cfg.brandShort} does not hide that behind vague recycling language.</p></div></section><section className={styles.body}><div className={`ever-shell ${styles.content}`}><section><h2>Standard recycling is not automatically certified destruction</h2><p>Do not assume ordinary electronics recycling includes certified sanitization, witnessed destruction, chain-of-custody documentation, or a certificate. Those requirements must be specifically confirmed before handoff.</p></section><section><h2>Before recycling a device</h2><p>Back up anything you need, sign out of accounts, remove activation locks when possible, and identify drives or devices that require special handling.</p></section><section><h2>Business inventory helps</h2><p>For commercial loads, upload photos, a CSV/Excel inventory, or a PDF when submitting the pickup request. That makes it easier to identify servers, drives, computers, and other data-bearing equipment before scheduling.</p></section><section><h2>Future secure-data services</h2><p>If {cfg.brandShort} adds a documented sanitization or physical-destruction service, the website will publish the actual method and documentation offered. We will not advertise certifications or destruction standards that operations do not support.</p></section><div className={styles.policyLinks}><Link href={`${r.services}/hard-drive-recycling`}>Hard drive recycling</Link><Link href={r.business}>Submit a business load</Link><Link href={r.contact}>Discuss a data requirement</Link></div></div></section></div>}
