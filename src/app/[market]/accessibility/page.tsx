import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getMarket,isMarketKey,marketRoutes } from "@/data/markets";
import styles from "../../legal.module.css";
export async function generateMetadata({params}:{params:Promise<{market:string}>}):Promise<Metadata>{const{market}=await params;const cfg=getMarket(market);if(!cfg)return{};return{title:`Accessibility | ${cfg.brandShort} ${cfg.marketLabel}`,description:`Accessibility statement for the ${cfg.brandShort} ElectronicRecycle.net experience.`,alternates:{canonical:`/${market}/accessibility`}}}
export default async function Page({params}:{params:Promise<{market:string}>}){const{market}=await params;if(!isMarketKey(market))notFound();const cfg=getMarket(market)!;const r=marketRoutes(market);return <div className={styles.page}><section className={styles.hero}><div className="ever-shell"><h1>Accessibility</h1><p>{cfg.brandShort} wants ElectronicRecycle.net to be usable by as many people as possible.</p></div></section><section className={styles.body}><div className={`ever-shell ${styles.content}`}><section><h2>Our approach</h2><p>The site uses semantic headings and landmarks, keyboard-operable controls, visible focus states, labeled form fields, text alternatives where images convey information, and reduced-motion support.</p></section><section><h2>Report an issue</h2><p>Use the <Link href={r.contact}>contact page</Link> and include the page, browser, device, and a short description of the problem if possible.</p></section></div></section></div>}
