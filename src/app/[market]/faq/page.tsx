import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getMarket, isMarketKey, marketRoutes } from "@/data/markets";
import styles from "../../legal.module.css";
export async function generateMetadata({params}:{params:Promise<{market:string}>}):Promise<Metadata>{const{market}=await params;const cfg=getMarket(market);if(!cfg)return{};return{title:`Electronics Recycling FAQ in ${cfg.marketLabel} | ${cfg.brandShort}`,description:`Answers about e-waste, computer recycling, TV recycling, pickup, and accepted electronics with ${cfg.brandShort}.`,alternates:{canonical:`/${market}/faq`}}}
export default async function Page({params}:{params:Promise<{market:string}>}){const{market}=await params;if(!isMarketKey(market))notFound();const cfg=getMarket(market)!;const r=marketRoutes(market);const qs=[{q:`What electronics does ${cfg.brandShort} accept?`,a:"Laptops, desktops, servers, networking equipment, drives, phones, tablets, monitors, components, printers, gaming systems, cables, power equipment, cameras, and other published electronics."},{q:"Do you accept TVs?",a:"Flat-screen LED TVs are accepted for $15 each. CRT and tube televisions are not accepted."},{q:"Do you offer business pickup?",a:market==="az"?"Yes. Arizona commercial pickup is free with at least 10 qualifying items. Smaller loads can use drop-off or distance-based pickup.":"Yes. El Paso business pickup requests are reviewed by load size, equipment mix, and location before scheduling."},{q:"Can I request residential pickup?",a:"Yes. Use the contact form with your ZIP code and item list so the local market can confirm availability and pricing."},{q:"What about data on hard drives?",a:"Do not assume ordinary recycling includes certified data sanitization or destruction. Back up and protect your data, and discuss special data-handling requirements before handoff."}];const schema={"@context":"https://schema.org","@type":"FAQPage",mainEntity:qs.map(i=>({"@type":"Question",name:i.q,acceptedAnswer:{"@type":"Answer",text:i.a}}))};return <div className={styles.page}><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className={styles.hero}><div className="ever-shell"><h1>{cfg.brandShort} electronics recycling FAQ</h1><p>Fast answers about accepted electronics, TVs, pickup, business loads, and data-bearing devices.</p></div></section><section className={styles.body}><div className={`ever-shell ${styles.content}`}>{qs.map(i=><section key={i.q}><h2>{i.q}</h2><p>{i.a}</p></section>)}<div className={styles.policyLinks}><Link href={r.whatWeAccept}>What we accept</Link><Link href={r.pricing}>Free vs. fee</Link><Link href={r.contact}>Contact {cfg.brandShort}</Link></div></div></section></div>}
