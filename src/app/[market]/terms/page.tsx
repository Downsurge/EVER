import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getMarket,isMarketKey,marketRoutes } from "@/data/markets";
import styles from "../../legal.module.css";
export async function generateMetadata({params}:{params:Promise<{market:string}>}):Promise<Metadata>{const{market}=await params;const cfg=getMarket(market);if(!cfg)return{};return{title:`Terms of Service | ${cfg.brandShort} ${cfg.marketLabel}`,description:`Website terms for ${cfg.brandShort} on ElectronicRecycle.net.`,alternates:{canonical:`/${market}/terms`}}}
export default async function Page({params}:{params:Promise<{market:string}>}){const{market}=await params;if(!isMarketKey(market))notFound();const cfg=getMarket(market)!;const r=marketRoutes(market);return <div className={styles.page}><section className={styles.hero}><div className="ever-shell"><h1>Terms of Service</h1><p>Terms that apply when using the {cfg.brandShort} website experience.</p><span className={styles.updated}>Last updated August 17, 2026</span></div></section><section className={styles.body}><div className={`ever-shell ${styles.content}`}><section><h2>Website information</h2><p>The website provides current operating information, but acceptance, pickup, pricing, and downstream options can change. A submitted pickup request is not a confirmed appointment or contract until the local market confirms it.</p></section><section><h2>Value recovery</h2><p>Evaluation for reuse, resale, component recovery, or material recovery is not a promise that equipment has value and is not a promise of payment.</p></section><section><h2>Data-bearing devices</h2><p>Unless a specific data service is expressly confirmed in writing, customers remain responsible for backing up and protecting data before handoff. Ordinary recycling should not be assumed to include certified data destruction.</p></section><section><h2>Lawful use</h2><p>You may not submit false information, malicious files, stolen equipment, or material you do not have authority to transfer.</p></section><section><h2>Contact</h2><p>Questions about these terms can be submitted through the <Link href={r.contact}>contact page</Link>.</p></section></div></section></div>}
