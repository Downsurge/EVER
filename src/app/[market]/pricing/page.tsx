import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getMarket, isMarketKey, marketRoutes } from "@/data/markets";
import styles from "../../legal.module.css";
export async function generateMetadata({params}:{params:Promise<{market:string}>}):Promise<Metadata>{const{market}=await params;const cfg=getMarket(market);if(!cfg)return{};return{title:`Electronics Recycling Pricing & Fees in ${cfg.marketLabel} | ${cfg.brandShort}`,description:`See free electronics recycling, LED TV fees, commercial pickup rules, and pickup pricing with ${cfg.brandShort}.`,alternates:{canonical:`/${market}/pricing`}}}
export default async function Page({params}:{params:Promise<{market:string}>}){const{market}=await params;if(!isMarketKey(market))notFound();const cfg=getMarket(market)!;const r=marketRoutes(market);return <div className={styles.page}><section className={styles.hero}><div className="ever-shell"><p className="ever-eyebrow">FREE VS. FEE</p><h1>Know the cost before you recycle.</h1><p>{cfg.brandShort} keeps common pricing and pickup rules in one place so customers do not have to request a quote just to understand the basics.</p></div></section><section className={styles.body}><div className={`ever-shell ${styles.content}`}><section><h2>Free for most accepted electronics</h2><p>Computers, laptops, servers, networking equipment, drives, phones, tablets, computer components, cables, gaming systems, and many other accepted electronics do not carry a standard recycling fee.</p></section><section><h2>LED TVs: $15 each</h2><p>Flat-screen LED televisions are accepted for <strong>$15 per TV</strong>. Keep the screen intact for handoff.</p></section><section><h2>CRT TVs and monitors</h2><p><strong>Not accepted.</strong> Tube/CRT displays require a different downstream recycling route.</p></section><section><h2>Commercial pickup</h2><p>{market==="az"?"Free commercial pickup is available with at least 10 qualifying items. Under 10 items, use free drop-off for eligible electronics or request pickup priced by distance.":"EPER reviews commercial pickup requests by equipment mix, quantity, access, and distance. Submit the load first so pickup and any fee can be confirmed."}</p></section><section><h2>Residential pickup</h2><p>Residential pickup is quoted based on distance and the equipment being collected. Send your ZIP code and item list before scheduling.</p></section><div className={styles.policyLinks}><Link href={r.residential}>Check an item</Link><Link href={r.business}>Plan a business pickup</Link><Link href={r.contact}>Ask about pickup pricing</Link></div></div></section></div>}
