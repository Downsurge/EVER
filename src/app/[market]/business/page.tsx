import Image from "next/image";
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { PickupBrief } from "@/components/PickupBrief";
import { getMarket, isMarketKey } from "@/data/markets";
import styles from "../../page.module.css";
export async function generateMetadata({params}:{params:Promise<{market:string}>}):Promise<Metadata>{const{market}=await params;const cfg=getMarket(market);if(!cfg)return{};return{title:`Business Electronics Recycling & E-Waste Pickup in ${cfg.marketLabel} | ${cfg.brandShort}`,description:`Commercial electronics recycling for computers, laptops, servers, networking gear, drives and office technology with ${cfg.brandShort}.`,alternates:{canonical:`/${market}/business`}}}
export default async function BusinessPage({params}:{params:Promise<{market:string}>}){const{market}=await params;if(!isMarketKey(market))notFound();const cfg=getMarket(market)!;return <div className={styles.businessPage}><section className={styles.businessHero}><div className={`ever-shell ${styles.businessHeroGrid}`}><div><h1>Business electronics recycling in {cfg.regionShort}.</h1><p>Submit mixed IT and office electronics in one structured request. Add photos or an inventory file so {cfg.brandShort} can review the load before scheduling.</p><ul className={styles.businessPolicyList}>{market==="az"?<><li>Free commercial pickup with 10 qualifying items</li><li>Under 10 items: free drop-off or distance-based pickup</li></>:<><li>Commercial pickup requests reviewed by load size and location</li><li>Drop-off and pickup details confirmed before scheduling</li></>}<li>Photos, CSV/Excel inventory lists, and PDFs can be attached</li></ul></div><div className={styles.businessHeroVisual} aria-hidden="true"><div className={styles.heroAssetGrid}>{[{src:"/brand/device-laptop.svg",label:"LAPTOPS"},{src:"/brand/device-server.svg",label:"SERVERS"},{src:"/brand/device-network.svg",label:"NETWORKING"},{src:"/brand/device-drives.svg",label:"DRIVES"}].map(a=><figure key={a.label} className={styles.heroAssetCard}><Image src={a.src} alt="" width={320} height={220} className={styles.heroAssetImage}/><figcaption>{a.label}</figcaption></figure>)}</div></div></div></section><section className={styles.businessBody}><div className="ever-shell"><PickupBrief market={market}/></div></section></div>}
